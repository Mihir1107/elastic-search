# --- Python ---
__pycache__/
*.py[cod]
.venv/
.python-version.local
.pytest_cache/
.ruff_cache/
.mypy_cache/
*.egg-info/

# --- Secrets / local env ---
.env
certs/

# --- Data & models (never commit; see CLAUDE.md) ---
data/
# Pooled candidates carry raw email bodies; regenerate with `make eval-pool`.
eval/pools.json
models/
*.tar.gz
# HuggingFace / torch caches live in ~/.cache by default; keep any local ones out too
.cache/

# --- Elasticsearch bind data (if ever used) ---
esdata*/

# --- Node / Next.js (Phase 5) ---
web/node_modules/
web/.next/
web/out/
web/*.tsbuildinfo
web/.env.local

# --- OS / editor ---
.DS_Store
*.swp
.idea/
.vscode/

# --- Local env + browser automation artifacts ---
.env.local
.env*.local
.playwright-mcp/
*.jpeg
tsconfig.tsbuildinfo
web/tsconfig.tsbuildinfo
